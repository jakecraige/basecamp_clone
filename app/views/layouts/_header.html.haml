%header
  .navbar
    .navbar-inner
      %a.brand{href: projects_path} BasecampClone
      %ul.nav
        - if user_signed_in?
          %li= link_to "My Projects", projects_path
          %li= link_to "My Profile", current_user
          %li= link_to "My Settings", edit_user_registration_path(current_user)
          %li= link_to "Sign Out", destroy_user_session_path
        - else
          %li= link_to "Sign In", new_user_session_path

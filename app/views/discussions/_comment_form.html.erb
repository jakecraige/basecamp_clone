<%= simple_form_for(@comment, url: project_discussion_comments_path(params[:project_id], params[:id]) ) do |f| %>

  <div class="form-inputs">
    <%= f.input :body %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
